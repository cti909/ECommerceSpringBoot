package com.example.DemoSpringBootAPI.Repository.Interfaces;

import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

import com.example.DemoSpringBootAPI.Data.Entities.Product;

public interface IProductCustomRepository {
//	Page<Product> filterProducts(Long categoryId, Long colorId, String size, Long priceMin, Long priceMax, Long styleId, Pageable pageable);
}
